{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ CMake Debug (LLDB)",
            "type": "cppdbg",
            "request": "launch",
            "program": "arch -arm64 lldb ${workspaceFolder}/build/main",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/build",
            "environment": [],
            "externalConsole": true,
            "MIMode": "lldb",
            "preLaunchTask": "build_with_ninja",
            
        }
    ]
}